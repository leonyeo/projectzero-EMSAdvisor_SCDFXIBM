import React from 'react'
import { Typography } from "@material-ui/core";

const Diagnosis = () => {

    return(
        <Typography>Hello, welcome to the 3rd Screen</Typography>
    );
};

export default Diagnosis;